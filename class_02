const number1= 1;  // não muda nunca, não pode ser modificado - never change, cannot be changed
let number2 = 2; // número variável -  can be changed

if(number1 === 1) {
    number2 = 6
}

console.log( 'your result is:', number2)
