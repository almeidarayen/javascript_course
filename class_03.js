const age = 8;
const name = "Rayen";

if ( age >= 18 || name === "Rayen") {
    console.log('This usuary can enter')
} else {
    console.log("No entry")
}

// = um igual é uma atribuição dois == é um comparativo e === é um comparativo mais preciso
